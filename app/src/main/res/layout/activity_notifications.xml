<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/Background">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbarNotifications"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/Primary"
            app:title="Stock Alerts"
            app:titleTextColor="@color/OnPrimary" />

    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:padding="16dp">

        <!-- SMS permission card for when permission is not granted -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardSmsPermission"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="4dp"
            app:strokeColor="@color/Accent"
            app:strokeWidth="2dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <!-- icon + title-->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginBottom="12dp">

                    <ImageView
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:src="@drawable/sms_icon"
                        android:contentDescription="@string/sms_icon"
                        app:tint="@color/Accent" />

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_weight="1"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:layout_marginStart="12dp">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/enable_sms_alerts"
                            android:textSize="16sp"
                            android:textStyle="bold"
                            android:textColor="@color/TextPrimary" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/get_notified_when_items_reach_zero_stock"
                            android:textSize="13sp"
                            android:textColor="@color/TextSecond"
                            android:layout_marginTop="2dp" />

                    </LinearLayout>

                </LinearLayout>

                <!-- permission explanation -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/permission_explanation"
                    android:textSize="14sp"
                    android:textColor="@color/TextSecond"
                    android:lineSpacingMultiplier="1.4"
                    android:layout_marginBottom="16dp" />

                <!-- enable button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnEnableSms"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:text="@string/enable_sms_notifications"
                    app:backgroundTint="@color/Accent"
                    android:textColor="@color/PrimaryDark"
                    android:layout_marginBottom="8dp" />

                <!-- skip button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnSkipSms"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="match_parent"
                    android:layout_height="40dp"
                    android:text="@string/continue_without_sms_notifications"
                    android:textColor="@color/TextSecond"
                    android:textSize="12sp" />

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- permission Denied for when user denies -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardSmsDenied"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:visibility="gone"
            app:cardCornerRadius="8dp"
            app:cardElevation="2dp"
            app:strokeColor="@color/Error"
            app:strokeWidth="1dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:padding="16dp"
                android:gravity="center_vertical">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/warning_icon"
                    android:contentDescription="@string/warning"
                    app:tint="@color/Error" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:text="@string/sms_is_disabled"
                    android:textSize="13sp"
                    android:textColor="@color/Error"
                    android:layout_marginStart="12dp" />

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!--header -->
        <TextView
            android:id="@+id/tvAlertsHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/out_of_stock_alerts"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/TextPrimary"
            android:layout_marginBottom="8dp" />

        <!-- empty alerts -->
        <LinearLayout
            android:id="@+id/layoutNoAlerts"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingVertical="40dp">

            <ImageView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:src="@drawable/notif_icon"
                android:alpha="0.3"
                android:contentDescription="@string/no_alerts" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/all_items_are_stocked"
                android:textSize="16sp"
                android:textColor="@color/TextSecond"
                android:layout_marginTop="12dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/alerts_appear_here_when_any_item_hits_zero"
                android:textSize="13sp"
                android:textColor="@color/TextSecond"
                android:layout_marginTop="4dp" />

        </LinearLayout>

        <!-- alert recyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvAlerts"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="gone" />

    </LinearLayout>

    <!-- nav bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/Surface"
        app:menu="@menu/nav_bar"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        android:elevation="8dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>